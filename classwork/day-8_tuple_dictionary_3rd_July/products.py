name = ()
price = ()
for i in range(10):
    n = input ("Enter product name:")
    p = int (input("Enter product price: "))

    name = name + (n,)
    price = price + (p,)
#------------------------------------------------------------    
low = min(price)
high = max (price)

print ("Lowest Price Product : ", name )

index (law)], "-", low)

print ("Highest Price Product:", name [price. index (high)], high)

count = 0

for i in price:

    if i > 4000:

        count+= 1

print ("Products with price greater than 4000:", count)